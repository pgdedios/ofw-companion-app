<turbo-stream target="place_<%= @saved_center.place_id %>" action="replace">
  <template>
    <button disabled class="mt-4 w-full px-4 py-2 bg-gray-300 text-gray-700 rounded text-sm cursor-not-allowed">
      Already Saved
    </button>
  </template>
</turbo-stream>